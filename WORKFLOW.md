# 开发协作流程

## 提交与推送规则

- **任何本地修改完成后，禁止自行提交（commit）或推送（push）。**
- 修改完成后先告知作者，由作者确认无误后，再执行提交与推送。
- 作者确认前，所有改动只保留在工作区/本地，不进入 git 历史，也不上传远程。

## 其他约定

- 涉及软依赖（SX-Attribute、CrazyEnchantments、MythicMobs、MythicCrucible 等）的配置或代码调整，先说明影响范围。
- 冒烟测试默认在本地 Folia 测试服进行；测试完成后及时清理测试服，避免长期挂载插件拖慢运行速度。
- README（中文/英文）需要与代码行为保持同步；纯配置类问题原则上不改代码做兼容，由配置方调整。
